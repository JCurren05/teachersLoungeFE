import React from "react";

class Friend {
  constructor(email, firstName, lastName, schoolId, role) {
    this.email = email;
    this.firstName = firstName;
    this.lastName = lastName;
    this.schoolId = schoolId;
    this.role = role;
  }
}

export default Friend;
