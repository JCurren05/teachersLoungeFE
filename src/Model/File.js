
//Defines a file, used as a single source of truth to keep things consistent
class File {
    constructor(url,name,type) {
      this.url = url;
      this.name = name;
      this.type= type;
    }
  }
  
  export default File;